import 'package:flutter/material.dart';

/// A class to hold styles and colors for the library.
class LibStyles {
  /// The color used for the outer ring of the circular progress indicator.
  /// It is a dark grey color.
  static const ringColor = Color(0xff27292c);
}
